import { WinstonLogger } from '../implementations/WinstonLogger.js';
export function createLogger(service, context, config) {
    return new WinstonLogger(context || {}, { ...config, service });
}
export function createTestLogger(service = 'test', context) {
    return new WinstonLogger(context || {}, { service, test: true });
}
//# sourceMappingURL=createLogger.js.map